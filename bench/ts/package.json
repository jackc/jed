{
  "name": "jed-bench-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Benchmark harness for the TS jed core vs PostgreSQL (porsager postgres) and SQLite (node:sqlite). Separate package: impl/ts stays dependency-free (CLAUDE.md §14, spec/design/benchmarks.md §7).",
  "scripts": {
    "test": "node --test tests/*.test.ts"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "smol-toml": "^1.3.1"
  }
}
